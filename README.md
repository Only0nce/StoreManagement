# StoreManagement CI Validation

This repository is used to run the Windows release gate for the StoreManagement WPF/.NET 10 project.

The validated source archive is expanded by GitHub Actions and must pass clean build, automated tests, WPF UI smoke tests, publish, and installer packaging before a release artifact is produced.
